# Database migrations

This folder will contain Alembic migrations. To initialize Alembic later:

```bash
pip install alembic
alembic init migrations
```

Configure `alembic.ini` and `env.py` to use `app.db.session` engine.
# Database migrations

This folder will contain Alembic migrations. To initialize Alembic later:

```bash
pip install alembic
alembic init migrations
```

Configure `alembic.ini` and `env.py` to use `app.db.session` engine.

